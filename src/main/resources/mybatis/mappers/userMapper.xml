<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.springboard.mapper.UserMapper">

    <insert id="insertUser">
        insert into SPRING_USER ( user_id ,username, password, email, registerDate, updateDate)
        values (user_id_seq.nextval, #{password},#{username},#{email}, sysdate, sysdate)
    </insert>
</mapper>